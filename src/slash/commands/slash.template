const SlashCommand = require("../../struct/slash-commands.js");
const { MessageEmbed } = require("discord.js");

class TemplateCommand extends SlashCommand {
  constructor() {
    super({
      id: "",
      description: "",
      ratelimit: 5
    });
  }

  async do(message) {
    try {
      //code here
    } catch (e) {
      throw new Error(e);
    }
  }
}

module.exports = TemplateCommand;